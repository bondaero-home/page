{
  "name": "bondaerohome",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/normalize-dist.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "workers:dev": "wrangler dev",
    "workers:deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.11",
    "@astrojs/sitemap": "^3.4.0",
    "astro": "^5.13.0",
    "sharp": "^0.34.2"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
